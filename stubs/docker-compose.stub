version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: {{IMAGE_NAME}}
    ports:
      - "{{PORT}}:9001"
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    restart: always
